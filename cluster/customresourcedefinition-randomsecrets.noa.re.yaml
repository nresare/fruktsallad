# Source: randomsecret
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: randomsecrets.noa.re
  annotations:
    noa.re/deploy-id: 5d0b9439ed204139
spec:
  group: noa.re
  names:
    kind: RandomSecret
    plural: randomsecrets
    singular: randomsecret
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for RandomSecretSpec via `CustomResource`
        properties:
          spec:
            properties:
              secrets:
                description: |-
                  The entries of the Secret that this RandomSecret manages. The Secret is
                  created with the same name and namespace as this resource.
                items:
                  properties:
                    length:
                      description: |-
                        Length in characters of the generated value. When omitted, the value is
                        made long enough to contain at least 256 bits of entropy.
                      format: uint32
                      minimum: 1.0
                      nullable: true
                      type: integer
                    name:
                      description: The key under which the generated value is stored
                        in the Secret.
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - secrets
            type: object
          status:
            nullable: true
            properties:
              observedGeneration:
                description: The value of `.metadata.generation` that was most recently
                  reconciled.
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: RandomSecret
        type: object
    served: true
    storage: true
    subresources:
      status: {}
